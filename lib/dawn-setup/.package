(package dawn-setup
  
  (root "src")
  (products (dawn-setup (title "Dawn Setup")
                        (run dawn-setup.run)
                        (build (dawn-setup executable: "Setup" minimum-heap: 500000 resources: "resources"))
                        (update dawn-setup.run)
                        (dependencies jiri)))
  (project ".project"))
