(package dawn-setup
  
  (root "src")
  (products (dawn-setup (title "Dawn Setup")
                        (run dawn-setup.run)
                        ;; console?: #t to test on windows
                        (build (dawn-setup executable: "Setup" minimum-heap: 500000 resources: "resources" console?: #t))
                        (update dawn-setup.run
                                dawn-setup.settings)
                        (dependencies jiri)))
  (project ".project"))
